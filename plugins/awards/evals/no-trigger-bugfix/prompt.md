---
description: "A React state bug must not invoke any awards skill."
max_turns: 6
allowed_tools: [Read, Glob, Grep, Skill]
---

Fix this React bug: a counter created with useState(0) inside a modal resets every time the modal closes and reopens. The count should persist across opens. Explain the fix.
