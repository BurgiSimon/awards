---
description: "Accessibility repairs on an internal settings form have no award framing and must not invoke any awards skill."
max_turns: 6
allowed_tools: [Read, Glob, Grep, Skill]
---

Our internal settings form failed an accessibility audit. The toggles are bare divs with click handlers, the error text is only colour, and the tab order jumps from the sidebar into the save button. Fix those three and show me the markup.
