---
description: "A WebGL layer over existing images: DOM-tethered planes, DPR cap, disposal, aria-hidden canvas, images retained, reduced-motion path."
max_turns: 80
timeout_seconds: 1800
allowed_tools: [Read, Glob, Grep, Skill, Write, Edit, Bash, AskUserQuestion]
---

Add a WebGL hero to this product page where the three product images distort with scroll velocity, the way the creative-developer sites do it. The images must stay real images in the DOM. Handle devices without WebGL and users who prefer reduced motion. If a tool is unavailable (npm, browser capture), say so and continue.
